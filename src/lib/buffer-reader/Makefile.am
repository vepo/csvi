# The libraries to build
noinst_LIBRARIES = libbufferreader.a
noinst_HEADERS = buffer-reader.h

# The files to add to the library and to the source distribution
bufferreader_a_SOURCES = \
		      $(bufferreader_a_HEADERS) \
                      buffer-reader.c
